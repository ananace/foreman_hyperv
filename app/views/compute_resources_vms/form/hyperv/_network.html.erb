<%= text_f f, :name, :label => _('Name'), :label_size => "col-md-3" %>
<%
    switches = compute_resource.switches.map { |s| [s.name, s.name] }
%>
<%= select_f f, :switch_name, switches, :first, :first, {include_blank: true}, label: _('Network switch'), :label_size => "col-md-3" %>
